-----------待解决的知识点-------
20/5/4
1.线程模型使用以及配置
<dubbo:protocol name="dubbo" dispatcher="all" threadpool="fixed" threads="100" />
2.文件配置：
1）通过-D参数指定  2）通过文件映射使用

20/4/1
1.解决处理管理控制台 404页面问题
2.zk上用命令清除不需要节点（清除历史节点：若没有节点，回去zk注册；风险点（若误删了正在使用的服务，需要整个集群服务重启，影响比较大）
怎样做到既不误删，也不保留历史的持久节点。持久话的节点怎么处理，是否耗费磁盘空间
或者容器启动时，检测是否有无用的节点，有就删除，但比较局限，因为当前节点没有，其它节点可能启动会有）